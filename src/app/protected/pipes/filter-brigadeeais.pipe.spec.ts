import { FilterBrigadeeaisPipe } from './filter-brigadeeais.pipe';

describe('FilterBrigadeeaisPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBrigadeeaisPipe();
    expect(pipe).toBeTruthy();
  });
});
