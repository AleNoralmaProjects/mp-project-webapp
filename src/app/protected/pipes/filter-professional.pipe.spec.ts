import { FilterProfessionalPipe } from './filter-professional.pipe';

describe('FilterProfessionalPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProfessionalPipe();
    expect(pipe).toBeTruthy();
  });
});
